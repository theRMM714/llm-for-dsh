# AGENTS.md

> 本文件仅保存全仓、长期、必须始终生效的规则。专项细则写入对应权威文档；开展专项工作前必须阅读并遵照该文档。

## 一、效力与优先级

- 本文件在整个仓库内生效。
- 本文件与其它项目文档冲突时，以本文件为准，并向用户指出冲突。
- 用户要求与本文件冲突时，先说明冲突和影响；只有用户明确确认后才执行。
- 不得隐瞒不确定性。能通过仓库、工具、测试或日志取得证据时先调查；证据仍不足且歧义会影响结果时，直接询问用户。

## 二、项目不变量

- 使用简体中文与用户交流。
- 项目始终是 GREEN FIELD，只维护当前唯一实现。
- 不为旧版本、已删除实现或假设中的其它仓库保留兼容层、废代码或历史说明。
- 不擅自引入版本迁移、版本号规划或兼容策略。
- 项目内工具链和环境应该始终使用最新稳定版，如版本落后应向用户说明，获得授权后进行更新。
- 默认同时支持 Windows、macOS、Linux；设计和实现必须先确认跨平台成立。
- 项目理念和当前契约优先于兼容旧行为。

## 三、路径硬约束

- 代码和文档中的项目内路径一律以产品根仓库根为基准，使用相对路径。
- 禁止写入带盘符的 Windows 路径、Unix 用户目录等机器相关绝对路径。
- 代码必须使用路径组件拼接，例如 `root.join(".home").join("providers.yaml")`；禁止把 `/` 或 `\` 写进字符串后拼接文件路径。
- 仓库内文件引用使用 `README.md`、`modules/<id>/module.yaml` 等相对形式。
- 依赖、工具链和配置默认收敛在项目目录内；确需使用项目外目录时，先说明原因并取得用户同意。

以下情况不属于机器绝对路径违规：

1. `.gitignore` 中行首 `/` 和 `**/` 的 Git 语义；
2. URL 路径，例如 `{base_url}/chat/completions`；
3. 以产品根为锚、可通过环境变量整体迁移的 `.home/` 和模块 `userdata/`。

## 四、代码与文档约束

- 遵守 DIP，保持高内聚、低耦合，按业务功能垂直切分。
- 业务之间通过 API 契约协作，不直接依赖其它业务的内部实现。
- 任意行为和实现必须通过 `TESTING.md` 规定的适用测试；不可测试的行为属于设计或测试缺口，必须如实记录。
- 注释只描述当前代码的功能、约束和联动模块。
- 长期文档只保留当前状态，不写过程、旧实现复盘或变更历史；历史由 Git、PR、issue 和 CI 报告承载。
- 不生产无实际约束、行为或使用价值的文档。

## 五、请求分类

| 类型 | 判据 | 处理方式 |
| --- | --- | --- |
| 咨询或审查 | 只调查、解释、总结、评审，不修改项目 | 直接取得证据并回答 |
| 任务 | 新功能、重构，或改变行为、契约、结构、依赖、数据模型、目录或设计 | 先给整体方案，等用户同意后实施 |
| 低级 bug | 拼写、变量、局部笔误或明显边界错误，且不改变接口、架构、行为设计、数据模型、依赖或目录 | 直接修复 |
| 非低级 bug | 涉及架构、设计、接口、数据、并发、安全、权限或依赖 | 先说明现象、证据、影响、可能根因和候选方案，由用户决定 |
| 不确定 | 证据不足，或分类会影响修改方式 | 按非低级 bug 处理，先询问用户 |

补充规则：

- 混合请求按最高风险类别处理。
- 涉及真实数据、权限或线上环境时，无论是否看似低级，都先取得用户确认。
- 用户批准的方案在约定范围和验收标准内持续有效，不重复请求批准；范围、接口、数据、安全、依赖或方案发生实质变化时，必须重新确认。
- 用户已明确要求执行某个具体方案时，视为该方案已获批准。
- 开发中发现不属于当前范围的新问题时，不混入本次修改；向用户说明并另行处理。

## 六、任务与开发流程

### 任务开始

1. 给出目标、范围、方案、风险和验收标准，等待用户同意。
2. 阅读并遵照本文件“文档路由”要求的专项权威文档。
3. 在对应权威文档中写明已批准的目标契约和验收标准；尚未实现的内容必须明确标记为实施中，不得写成当前能力。
4. 只有任务引入新的长期领域且现有文档无法承载时，才创建新的专项文档；不得为单次任务创建无长期价值的文档。

### 实施

1. 严格按已批准范围开发，不擅自扩大修改。
2. 优先用工具和可观察证据判断，不凭猜测修改。
3. 遇到阻塞、外部依赖或方案需要实质变化时，停止并询问用户。
4. 原方案范围内暴露的实现错误可直接修复；证明原方案错误时，回到方案确认。

### 完成与销账

1. 按 `TESTING.md` 运行适用测试和质量检查；失败则继续修复，不得把 `env-skip`、`gap` 或 `blocked` 写成通过。
2. 将对应权威文档更新为实际当前状态，删除“实施中”、临时计划、已完成待办、过期和矛盾描述。
3. 确认代码、测试、报告和文档一致后再提交。

低级 bug 不需要任务方案和事前审批，但仍必须执行适用测试、文档同步、状态检查和本地提交。

## 七、Git

- 提交前检查 `.gitignore`，并用 `git status` 确认没有意外跟踪文件或无关修改。
- 不覆盖、删除或提交用户已有且不属于当前范围的工作区修改。
- 完成修改和适用测试后创建本地提交。
- 只有需要真机或 GitHub Actions 验证，或用户明确授权时，才允许推送；其它情况不得擅自 `git push`。
- actions的ci-report直接用git或git cli拉取，禁止轮询查网页。无法确认时机时委托用户拉取。

## 八、文档路由

专项要求具有强制性；工作涉及多个领域时，必读文档累加。
